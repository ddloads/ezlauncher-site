services:
  ezlauncher-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ezlauncher-site
    restart: unless-stopped
    environment:
      - PORT=${PORT:-3000}
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
